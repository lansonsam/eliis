# Project: Eliis

> 本文件是 Codex / Anthropic Agent 的项目级指令文件，
> 每次会话开始时会被自动加载。请所有 AI 助手严格遵守。

---

## 1. 目录隔离规则（最高优先级）

### 只读参考代码目录

下列目录均为**外部项目克隆**，仅作为 Eliis 的架构 / 实现 / 设计参考，
**不属于本项目源码**。隔离策略采用"**可读不可写**"模式：

| 目录 | 来源 | 参考用途 |
| --- | --- | --- |
| `new-api/` | [QuantumNous/new-api](https://github.com/QuantumNous/new-api) | 协议适配 / Relay 架构 / Channel 实现 |
| `eino/` | [cloudwego/eino](https://github.com/cloudwego/eino) | IR / Schema 设计、Stream 抽象、Agent 编排（仅借鉴） |

**隔离机制（三层）**：

| 层 | 文件 | 效果 |
| --- | --- | --- |
| Git | `.gitignore` | 不入库（配 `!*/.gitkeep` 留占位） |
| Cursor 索引 | `.cursorindexingignore` | 不参与 codebase 语义搜索（避免污染） |
| AI 行为 | **本文件（红线）** | Agent 自觉遵守"只读"约定 |

**Agent 必须遵守**：

- ✅ **允许**使用 `Read` / `Grep` / `Glob` / `Shell` 读取这些目录的内容作为参考
- ❌ **禁止**执行 `Write` / `Edit` / `MultiEdit` / `Bash` 写操作
- ❌ **禁止**创建、删除、重命名、移动其内任何文件或目录
- ❌ **禁止**对其内文件运行 `git add` / `git commit` / 格式化 / lint 自动修复
- 如果用户需求看起来需要修改这些目录，请**先停下来确认**：
  > "你要改的是参考代码 `<目录>/`，确认要动它吗？还是想在 Eliis 主项目里做对应改动？"

> ⚠️ Cursor 没有原生的"只读"机制，"不可写"完全靠本红线约束。
> 如果 Agent 违反，请把违规行为视为 bug，及时修正并复述本文件给会话开头。

### `.git/` —— 不要直接操作

不要直接读写 `.git/` 内部文件；所有 Git 操作请通过 `git` 命令完成。

---

## 2. 项目目标

Eliis 是一个全新的项目，**参考但不复用** New API 的设计。
具体技术栈、架构、需求待用户确认后补充到本文件。

---

## 3. 协作约定

- 所有源代码、配置、文档都放在仓库根目录（与 `new-api/` 平级），**不要**写进 `new-api/`
- 提交信息使用中文或英文均可，但需简明描述意图
- 涉及破坏性操作（删文件、重置分支、强制推送等）必须先经用户确认

---

## 4. 当本文件与用户当前指令冲突时

以**用户的显式指令**为准，但需要：

1. 先口头确认用户是否真的想绕过本文件的约束
2. 得到明确同意后才执行
3. 不要"沉默地"违反本文件约定
